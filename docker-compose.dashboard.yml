version: '3.8'
services:
  dashboard:
    build:
      context: ./middle
      dockerfile: Dockerfile.dashboard
    ports:
      - "8501:8501"
    networks:
      - litellm_network
    environment:
      - HOST_NAME=litellm_db
networks:
  litellm_network:
    external: true